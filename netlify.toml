[build]
  base = "."
  publish = "dist/public"
  command = "npx vite build --config vite.config.netlify.ts"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
